# game-review-system
game-review-system
# 初始化Git（如果还没初始化）
git init

# 添加所有文件到暂存区
git add .

# 提交更改
git commit -m "初始提交：游戏评价系统前端"

# 添加GitHub远程仓库
git remote add origin https://github.com/你的用户名/game-review-system.git

# 推送到GitHub
git branch -M main
git push -u origin main
